# Resume Builder

A project for generating resumes in PDF format using React.

## Features
- Form-based input for user data
- PDF generation
- Responsive UI

## Tech Stack
- React
- React-PDF
- HTML/CSS

## How to Use
1. Clone the repo.
2. Run `npm install`.
3. Start the app with `npm start`.

